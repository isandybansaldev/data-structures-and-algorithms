# Count Negative Numbers in an Array

Given an array of integers, write a function that counts how many elements in the array are negative numbers and returns that count.

```js
arr = [1, -2, 3, -5, 0, -8, 7, -9];
output = 4;
```
